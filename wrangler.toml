# wrangler.toml — Worker bixos avec assets statiques (frontend Vite)
# Déploiement: Frontend statique dans le Worker existant

name = "bixos"
compatibility_date = "2024-01-01"

# Assets statiques (build Vite → dist/)
[assets]
directory = "./dist"
