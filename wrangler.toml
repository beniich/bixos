# wrangler.toml — Worker bixos avec assets statiques et API Hono

name = "bixos"
main = "server.ts"
compatibility_date = "2026-09-10"
compatibility_flags = ["nodejs_compat"]

# Build : prisma generate + Vite frontend
# Wrangler bundle server.ts automatiquement (pas besoin d'esbuild)
[build]
command = "npx prisma generate && vite build"

# Résolution du client Prisma généré pour le bundler Wrangler
[alias]
".prisma/client/default" = "node_modules/.prisma/client/default"

# Assets statiques (build Vite → dist/)
[assets]
directory = "./dist"
binding = "ASSETS"

[[d1_databases]]
binding = "DB"
database_name = "bixos-db"
database_id = "94ddc43c-96d8-419d-8ecd-bd28f5e461a1"
